interface:
  display_name: "Deploy List Project"
  short_description: "Build and deploy the standalone project selector"
  default_prompt: "Use $deploy-list-project to build and deploy the standalone project selector."
